<%
# Select label from either config_label or from the namevar
if @config_label == :undef then
    label = @name
else
    label = @config_label
end
-%>
[<%= label %>]
<%= Array(@config).join("\n") %>
